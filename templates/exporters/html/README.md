# HTML Exporter

HTML is the main presentation runtime.

Use it for:

- live presentation
- keyboard/touch navigation
- presenter mode
- Canvas FX
- gesture control
- hosted static decks

Input should be `content-ir.json`. Select layouts and fill placeholders from IR instead of parsing raw source material again.

