# PDF Exporter

PDF export is a static artifact generated from the HTML deck or from Typst.

Use browser print/export when the user wants the HTML deck appearance preserved.
Use Typst when the user wants a handout, workbook, print-first report, or tighter page typography.

PDF output must not include presenter-only notes unless the user asks for a speaker-notes handout.

